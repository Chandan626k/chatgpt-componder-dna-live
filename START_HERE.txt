COMPOUNDER DNA — FIXED FRONTEND v2

What was fixed:
- All top navigation tabs now switch correctly.
- Analyse Stock click handler is initialized safely.
- Quick stock chips trigger analysis.
- Missing optional elements no longer crash the whole page during startup.
- Swing/Radar auto scan handles API array or {data:[...]} responses.
- Existing backend files are preserved.

Use the files in this package to replace the corresponding files in your existing GitHub repository. Do not copy node_modules. Do not delete the .git folder.
